# Administration is reachable from one address; everyone else gets 403.
allow YOUR.IP.HERE;    # replace with your address
allow 127.0.0.1;        # server itself, so an ssh tunnel still works
deny all;
